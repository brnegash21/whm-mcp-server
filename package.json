{
  "name": "whm-mcp-server",
  "version": "0.2.0",
  "description": "MCP server for the WHM (WebHost Manager) API 1 — server-level cPanel administration: accounts, domains, DNS, email, packages, SSL/AutoSSL, backups, resellers, cPHulk, PHP, logs",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "whm-mcp-server": "dist/index.js"
  },
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc && chmod +x dist/index.js",
    "clean": "rm -rf dist",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js",
    "verify:spec": "node scripts/verify-spec.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": ["mcp", "whm", "cpanel", "anthropic", "claude"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "axios": "^1.7.9",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
