export const CHARACTER_LIMIT = 25000;
export const REQUEST_TIMEOUT_MS = 60000; // WHM operations can be slow
export const DEFAULT_PAGE_SIZE = 25;
export const MAX_PAGE_SIZE = 100;

export enum ResponseFormat {
  MARKDOWN = "markdown",
  JSON = "json",
}
